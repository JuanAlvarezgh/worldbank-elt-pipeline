version: 2
models:
  - name: stg_countries
    columns:
      - name: country_iso3
        tests: [unique, not_null]
  - name: stg_indicator_values
    columns:
      - name: value
        tests: [not_null]
